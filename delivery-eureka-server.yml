server:
  port: 9091

eureka:
  instance:
    hostname: localhost
  client:
    registerWithEureka: true
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://[myIP@]:8787/eureka
  server:
    wait-time-in-ms-when-sync-empty: 3000