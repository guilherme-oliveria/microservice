server:
  port: 8761

eureka:
  instance:
    hostname: localhost
  client:
    registerWithEureka: true
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://[myIP@]:8761/eureka
  server:
    wait-time-in-ms-when-sync-empty: 4000